/** AlgoSpot challenges with snippets exclusive to AlgoSpot (not in Big O pool). */

function c(id, title, snippet, options, answer, difficulty, category, complexity, explanation, hint, reference, whatItIs) {
  return {
    id,
    title,
    snippet,
    options,
    answer,
    difficulty,
    category,
    caseNote: complexity,
    whatItIs,
    explanation,
    hint,
    reference,
    learnMoreURL: "https://www.geeksforgeeks.org/dsa/algorithms/",
  };
}

export const algoSpotExclusiveExtra = [
  c("exo-as-kmp", "KMP", "function kmpSearch(text, pattern):\n  lps = buildLPS(pattern)\n  i = 0; j = 0\n  while i < text.length:\n    if text[i] == pattern[j]:\n      i++; j++\n      if j == pattern.length: return i - j\n    else if j > 0: j = lps[j - 1]\n    else: i++\n  return -1", ["KMP", "Busca de substring", "Rabin-Karp", "Z-algorithm"], "KMP", "hard", "busca", "O(n)", "Usa tabela LPS para evitar retroceder no texto ao falhar o match.", "lps[j-1] reaproveita prefixo.", "https://www.geeksforgeeks.org/dsa/kmp-algorithm-for-pattern-searching/", "Busca de padrão em texto em O(n) usando prefixo próprio mais longo (LPS)."),
  c("exo-as-rabin-karp", "Rabin-Karp", "function rabinKarp(text, pattern):\n  pHash = 0; tHash = 0; h = 1\n  for i in 0..pattern.length - 1:\n    pHash = (pHash * 256 + pattern[i]) % 101\n    tHash = (tHash * 256 + text[i]) % 101\n    h = (h * 256) % 101\n  for i in 0..text.length - pattern.length:\n    if pHash == tHash and matchAt(text, i, pattern):\n      return i\n    tHash = rollHash(tHash, text, i, h, pattern.length)", ["Rabin-Karp", "KMP", "Busca de substring", "Z-algorithm"], "Rabin-Karp", "hard", "busca", "O(n)", "Compara hashes de janelas deslizantes antes de confirmar match literal.", "Rolling hash com h pré-calculado.", "https://www.geeksforgeeks.org/dsa/rabin-karp-algorithm-for-pattern-searching/", "Busca de padrão comparando hashes rolling — confirma colisões com comparação direta."),
  c("exo-as-monotonic-stack", "Monotonic stack", "function nextGreater(arr):\n  result = array(arr.length, -1)\n  stack = []\n  for i in 0..arr.length:\n    while stack not empty and arr[i] > arr[stack.top]:\n      idx = stack.pop()\n      result[idx] = arr[i]\n    stack.push(i)\n  return result", ["Monotonic stack", "Monotonic queue", "Sliding window", "Heap push"], "Monotonic stack", "medium", "padrao", "O(n)", "Pilha mantém índices em ordem decrescente para achar próximo maior.", "Pop enquanto arr[i] é maior.", "https://www.geeksforgeeks.org/dsa/next-greater-element/", "Pilha que mantém elementos em ordem monótona para resolver próximo maior/menor em O(n)."),
  c("exo-as-monotonic-queue", "Monotonic queue", "function slidingMax(arr, k):\n  deque = []\n  result = []\n  for i in 0..arr.length:\n    while deque not empty and arr[deque.back] <= arr[i]:\n      deque.popBack()\n    deque.pushBack(i)\n    if deque.front <= i - k:\n      deque.popFront()\n    if i >= k - 1:\n      result.push(arr[deque.front])\n  return result", ["Monotonic queue", "Monotonic stack", "Sliding window", "Heap pop"], "Monotonic queue", "hard", "padrao", "O(n)", "Deque mantém candidatos ao máximo da janela em ordem decrescente.", "Remove índices fora da janela.", "https://www.geeksforgeeks.org/dsa/sliding-window-maximum-maximum-of-all-subarrays-of-size-k/", "Fila dupla com ordem monótona para máximo/mínimo em janela deslizante."),
  c("exo-as-trie-search", "Trie", "function trieSearch(root, word):\n  node = root\n  for ch in word:\n    if ch not in node.children:\n      return false\n    node = node.children[ch]\n  return node.isEnd", ["Trie", "Hash linear probing", "Busca de substring", "Busca em BST"], "Trie", "medium", "hash", "O(m)", "Desce um nível por caractere da palavra — O(m) para palavra de tamanho m.", "children[ch] por letra.", "https://www.geeksforgeeks.org/dsa/trie-insert-and-search/", "Árvore de prefixos: cada nível representa um caractere para busca/inserção de strings."),
  c("exo-as-lru-cache", "LRU cache", "function lruGet(cache, key):\n  if key not in cache.map: return -1\n  node = cache.map[key]\n  moveToFront(cache, node)\n  return node.value\n\nfunction lruPut(cache, key, value):\n  if cache.size >= cache.capacity: evictTail(cache)\n  insertAtFront(cache, key, value)", ["LRU cache", "Hash linear probing", "Trie", "Union-Find"], "LRU cache", "hard", "padrao", "O(1)", "Hash map + lista dupla para mover o mais recente e expulsar o menos usado.", "moveToFront + evictTail.", "https://www.geeksforgeeks.org/dsa/lru-cache-implementation/", "Cache que remove o item menos recentemente usado ao atingir capacidade."),
  c("exo-as-two-sum-sorted", "Two pointers (ordenado)", "function twoSumSorted(arr, target):\n  left = 0\n  right = arr.length - 1\n  while left < right:\n    sum = arr[left] + arr[right]\n    if sum == target:\n      return [left, right]\n    if sum < target:\n      left++\n    else:\n      right--\n  return []", ["Two pointers (ordenado)", "Two sum", "Busca binária", "Sliding window"], "Two pointers (ordenado)", "medium", "padrao", "O(n)", "Em array ordenado, converge left/right conforme soma vs target.", "Soma guia qual ponteiro mover.", "https://www.geeksforgeeks.org/dsa/two-pointers-technique/", "Dois ponteiros nas extremidades de array ordenado para achar par com soma alvo."),
  c("exo-as-fisher-yates", "Fisher-Yates", "function shuffle(arr):\n  for i in arr.length - 1 down to 1:\n    j = randomInt(0, i)\n    swap(arr[i], arr[j])\n  return arr", ["Fisher-Yates", "Permutações", "Quick sort", "Heap sort"], "Fisher-Yates", "medium", "padrao", "O(n)", "Troca cada posição com índice aleatório à esquerda — permutação uniforme.", "j em [0, i] a cada passo.", "https://www.geeksforgeeks.org/dsa/shuffle-a-given-array-using-fisher-yates-shuffle-algorithm/", "Embaralha array em O(n) gerando permutação uniformemente aleatória."),
  c("exo-as-reservoir", "Reservoir sampling", "function reservoirSample(stream, k):\n  reservoir = first k items of stream\n  for i in k..stream.length:\n    j = randomInt(0, i)\n    if j < k:\n      reservoir[j] = stream[i]\n  return reservoir", ["Reservoir sampling", "Fisher-Yates", "Moore voting", "Quickselect"], "Reservoir sampling", "hard", "padrao", "O(n)", "Mantém amostra de tamanho k de fluxo desconhecido em uma passagem.", "Substitui reservoir[j] com prob. k/i.", "https://www.geeksforgeeks.org/dsa/reservoir-sampling/", "Seleciona k elementos aleatórios de um fluxo de tamanho desconhecido em O(n) e uma passagem."),
  c("exo-as-kosaraju", "Kosaraju SCC", "function kosaraju(adj, n):\n  order = []\n  dfsFillOrder(adj, order)\n  rev = reverseGraph(adj)\n  visited = set()\n  for u in reversed(order):\n    if u not in visited:\n      component = []\n      dfsCollect(rev, u, visited, component)", ["Kosaraju SCC", "Tarjan SCC", "Ordenação topológica", "DFS"], "Kosaraju SCC", "hard", "grafos", "O(n)", "Duas DFS: ordem de término e componentes no grafo transposto.", "reverseGraph + dfsCollect.", "https://www.geeksforgeeks.org/dsa/strongly-connected-components/", "Encontra componentes fortemente conexos com duas passagens DFS e grafo reverso."),
  c("exo-as-meet-middle", "Meet in the middle", "function subsetSumMiddle(arr, target):\n  left = allSubsetSums(arr[0..n/2])\n  right = allSubsetSums(arr[n/2..n])\n  sort(right)\n  for s in left:\n    if binarySearch(right, target - s):\n      return true\n  return false", ["Meet in the middle", "Power set (bitmask)", "Subset sum", "Three sum"], "Meet in the middle", "hard", "recursao", "O(2^(n/2))", "Divide o conjunto ao meio e combina somas de subconjuntos das metades.", "2^(n/2) por lado.", "https://www.geeksforgeeks.org/dsa/meet-in-the-middle/", "Divide problema exponencial ao meio, gera subconjuntos de cada metade e combina."),
  c("exo-as-ford-fulkerson", "Ford-Fulkerson", "function maxFlow(graph, source, sink):\n  flow = 0\n  while true:\n    path = bfsAugmentingPath(graph, source, sink)\n    if path is empty:\n      break\n    bottleneck = minCapacity(path)\n    augment(path, bottleneck)\n    flow = flow + bottleneck\n  return flow", ["Ford-Fulkerson", "Dijkstra", "Bellman-Ford", "BFS caminho mínimo"], "Ford-Fulkerson", "hard", "grafos", "O(n²)", "Encontra caminhos aumentantes com BFS (Edmonds-Karp) até saturar.", "bfsAugmentingPath + augment.", "https://www.geeksforgeeks.org/dsa/ford-fulkerson-algorithm-for-maximum-flow-problem/", "Calcula fluxo máximo repetindo caminhos aumentantes de fonte ao sumidouro."),
  c("exo-as-bucket-sort", "Bucket sort", "function bucketSort(arr, bucketCount):\n  buckets = array(bucketCount, [])\n  minVal = min(arr)\n  maxVal = max(arr)\n  range = maxVal - minVal + 1\n  for x in arr:\n    idx = floor((x - minVal) / range * bucketCount)\n    buckets[idx].push(x)\n  for bucket in buckets:\n    sort(bucket)\n  return flatten(buckets)", ["Bucket sort", "Counting sort", "Radix sort", "Merge sort"], "Bucket sort", "medium", "ordenacao", "O(n)", "Distribui em baldes e ordena cada balde — linear quando baldes são pequenos.", "floor para índice do balde.", "https://www.geeksforgeeks.org/dsa/bucket-sort-2/", "Ordenação que distribui elementos em baldes por faixa e ordena cada balde."),
  c("exo-as-shell-sort", "Shell sort", "function shellSort(arr):\n  n = arr.length\n  gap = n / 2\n  while gap > 0:\n    for i in gap..n:\n      temp = arr[i]\n      j = i\n      while j >= gap and arr[j - gap] > temp:\n        arr[j] = arr[j - gap]\n        j = j - gap\n      arr[j] = temp\n    gap = gap / 2", ["Shell sort", "Insertion sort", "Heap sort", "Bubble sort"], "Shell sort", "medium", "ordenacao", "O(n log n)", "Insertion sort com gaps decrescentes — melhora sobre O(n²) puro.", "gap /= 2 a cada rodada.", "https://www.geeksforgeeks.org/dsa/shellsort/", "Ordenação por inserção com intervalos (gaps) que vão encolhendo até 1."),
  c("exo-as-pancake-sort", "Pancake sort", "function pancakeSort(arr):\n  for size in arr.length down to 2:\n    maxIdx = indexOfMax(arr, 0, size)\n    flip(arr, 0, maxIdx)\n    flip(arr, 0, size - 1)", ["Pancake sort", "Selection sort", "Bubble sort", "Heap sort"], "Pancake sort", "hard", "ordenacao", "O(n²)", "Coloca o máximo no lugar com flip de prefixo — O(n) flips × n posições.", "flip + indexOfMax.", "https://www.geeksforgeeks.org/dsa/pancake-sorting/", "Ordena apenas com operações de reverter prefixo do array (panqueca)."),
  c("exo-as-bidirectional-bfs", "BFS bidirecional", "function bidirectionalBFS(start, goal, neighbors):\n  front = set(start); back = set(goal)\n  depth = 0\n  while front not empty and back not empty:\n    if front.size > back.size: swap(front, back)\n    next = set()\n    for node in front:\n      for nb in neighbors(node):\n        if nb in back: return depth + 1\n        if nb not in visited: visited.add(nb); next.add(nb)\n    front = next; depth++", ["BFS bidirecional", "BFS caminho mínimo", "Dijkstra", "DFS"], "BFS bidirecional", "hard", "grafos", "O(n)", "Expande duas fronteiras até se encontrarem — metade da profundidade na prática.", "front e back alternam.", "https://www.geeksforgeeks.org/dsa/bidirectional-search/", "BFS partindo simultaneamente de origem e destino até as fronteiras se encontrarem."),
  c("exo-as-id-dfs", "DFS iterativo aprofundando", "function iterativeDeepening(root, maxDepth):\n  for depth in 0..maxDepth:\n    if dfsLimited(root, 0, depth):\n      return true\n  return false", ["DFS iterativo aprofundando", "DFS", "BFS", "Busca binária"], "DFS iterativo aprofundando", "medium", "grafos", "O(n)", "Repete DFS com limite crescente de profundidade — completo como BFS em memória O(d).", "dfsLimited até depth.", "https://www.geeksforgeeks.org/dsa/iterative-deepening-searchids/", "DFS com limite de profundidade crescente — combina memória baixa com completude."),
  c("exo-as-fenwick", "Fenwick tree", "function prefixSum(tree, index):\n  total = 0\n  i = index\n  while i > 0:\n    total = total + tree[i]\n    i = i - (i & -i)\n  return total", ["Fenwick tree", "Prefix sum", "Segment tree", "Heap push"], "Fenwick tree", "hard", "arvore", "O(log n)", "Soma prefixo em árvore binária indexada subindo com i - (i & -i).", "i & -i isola bit mais baixo.", "https://www.geeksforgeeks.org/dsa/binary-indexed-tree-or-fenwick-tree-2/", "Árvore indexada binária para prefix sums e updates pontuais em O(log n)."),
  c("exo-as-segment-tree", "Segment tree", "function rangeQuery(tree, node, start, end, left, right):\n  if right < start or left > end:\n    return neutral\n  if left <= start and end <= right:\n    return tree[node]\n  mid = (start + end) / 2\n  return combine(\n    rangeQuery(tree, 2*node, start, mid, left, right),\n    rangeQuery(tree, 2*node+1, mid+1, end, left, right)\n  )", ["Segment tree", "Fenwick tree", "Prefix sum", "Merge sort"], "Segment tree", "hard", "arvore", "O(log n)", "Divide intervalo recursivamente para consulta em range.", "Dois filhos 2*node.", "https://www.geeksforgeeks.org/dsa/segment-tree-data-structure/", "Árvore de segmentos para consultas e updates em intervalos em O(log n)."),
  c("exo-as-manacher", "Manacher", "function longestPalindrome(s):\n  t = preprocess(s)\n  p = array(t.length, 0)\n  center = 0; right = 0\n  for i in 1..t.length - 1:\n    mirror = 2 * center - i\n    if i < right: p[i] = min(right - i, p[mirror])\n    while matchesPalindrome(t, i, p[i]): p[i]++\n    if i + p[i] > right: center = i; right = i + p[i]", ["Manacher", "Two pointers", "KMP", "Busca de substring"], "Manacher", "hard", "busca", "O(n)", "Expande palíndromos reutilizando informação simétrica em O(n).", "mirror = 2*center - i.", "https://www.geeksforgeeks.org/dsa/manachers-algorithm-linear-time-longest-palindromic-substring-part-1/", "Encontra palíndromo mais longo em O(n) com expansão centrada e espelhamento."),
  c("exo-as-z-algorithm", "Z-algorithm", "function zAlgorithm(s):\n  z = array(s.length, 0)\n  l = 0\n  r = 0\n  for i in 1..s.length - 1:\n    if i <= r:\n      z[i] = min(r - i + 1, z[i - l])\n    while i + z[i] < s.length and s[z[i]] == s[i + z[i]]:\n      z[i]++\n    if i + z[i] - 1 > r:\n      l = i\n      r = i + z[i] - 1\n  return z", ["Z-algorithm", "KMP", "Rabin-Karp", "Busca de substring"], "Z-algorithm", "hard", "busca", "O(n)", "Array Z de prefixos coincidentes com o início da string.", "Janela [l,r] reaproveita valores.", "https://www.geeksforgeeks.org/dsa/z-algorithm-linear-time-pattern-searching-algorithm/", "Constrói array Z de matches de prefixo em O(n) para busca de padrões."),
  c("exo-as-morris-inorder", "Morris traversal", "function morrisInorder(root):\n  current = root\n  while current != null:\n    if current.left == null:\n      visit(current); current = current.right\n    else:\n      pred = current.left\n      while pred.right != null and pred.right != current:\n        pred = pred.right\n      if pred.right == null:\n        pred.right = current; current = current.left\n      else:\n        pred.right = null; visit(current); current = current.right", ["Morris traversal", "Inorder DFS", "DFS", "Detecção de ciclo"], "Morris traversal", "hard", "arvore", "O(n)", "Percorre BST in-order sem stack extra usando links thread temporários.", "pred.right = current como thread.", "https://www.geeksforgeeks.org/dsa/inorder-tree-traversal-without-recursion-and-without-stack/", "Traversal in-order em árvore binária com O(1) espaço extra via links de predecessor."),
  c("exo-as-tarjan", "Tarjan SCC", "function tarjan(adj, u, index, stack, onStack):\n  index[u] = currentIndex++\n  low[u] = index[u]\n  stack.push(u)\n  onStack.add(u)\n  for v in adj[u]:\n    if index[v] is undefined:\n      tarjan(adj, v, index, stack, onStack)\n      low[u] = min(low[u], low[v])\n    else if v in onStack:\n      low[u] = min(low[u], index[v])\n  if low[u] == index[u]:\n    pop component from stack", ["Tarjan SCC", "Kosaraju SCC", "DFS", "Union-Find"], "Tarjan SCC", "hard", "grafos", "O(n)", "Uma DFS com low-link para extrair componentes fortemente conexos.", "low[u] == index[u] fecha SCC.", "https://www.geeksforgeeks.org/dsa/tarjan-algorithm-find-strongly-connected-components/", "Encontra SCCs em uma única DFS usando índices de descoberta e low-link."),
  c("exo-as-astar", "A*", "function aStar(start, goal, heuristic):\n  open = priorityQueue([(heuristic(start, goal), start)])\n  gScore[start] = 0\n  while open not empty:\n    f, current = open.popMin()\n    if current == goal: return gScore[current]\n    for neighbor in neighbors(current):\n      tentative = gScore[current] + cost(current, neighbor)\n      if tentative < gScore.get(neighbor, infinity):\n        gScore[neighbor] = tentative\n        open.push((tentative + heuristic(neighbor, goal), neighbor))", ["A*", "Dijkstra", "BFS caminho mínimo", "Bellman-Ford"], "A*", "hard", "grafos", "O(n log n)", "Melhor-primeiro com f = g + heurística admissível.", "open.popMin por f-score.", "https://www.geeksforgeeks.org/dsa/a-search-algorithm/", "Busca de caminho que usa custo real + heurística para priorizar nós promissores."),
  c("exo-as-comb-sort", "Comb sort", "function combSort(arr):\n  n = arr.length\n  gap = n\n  shrink = 1.3\n  sorted = false\n  while not sorted:\n    gap = max(1, gap / shrink)\n    sorted = true\n    for i in 0..n - gap:\n      if arr[i] > arr[i + gap]:\n        swap(arr[i], arr[i + gap])\n        sorted = false", ["Comb sort", "Bubble sort", "Shell sort", "Insertion sort"], "Comb sort", "medium", "ordenacao", "O(n²)", "Bubble sort com gap que encolhe por fator 1.3.", "gap / shrink até 1.", "https://www.geeksforgeeks.org/dsa/comb-sort/", "Melhoria do bubble sort comparando elementos separados por gap decrescente."),
  c("exo-as-top-k-heap", "Top K com heap", "function topK(arr, k):\n  heap = minHeap()\n  for x in arr:\n    heap.push(x)\n    if heap.size > k:\n      heap.pop()\n  return heap.toArray()", ["Top K com heap", "Heap sort", "Quickselect", "Moore voting"], "Top K com heap", "medium", "heap", "O(n log k)", "Min-heap de tamanho k mantém os k maiores vistos.", "pop quando size > k.", "https://www.geeksforgeeks.org/dsa/k-largestor-smallest-elements-in-an-array/", "Mantém heap de tamanho k para extrair os k maiores elementos eficientemente."),
  c("exo-as-dijkstra-heap", "Dijkstra com heap", "function dijkstraHeap(adj, source):\n  dist = map(source -> 0)\n  pq = minHeap([(0, source)])\n  while pq not empty:\n    d, u = pq.popMin()\n    if d > dist[u]:\n      continue\n    for edge in adj[u]:\n      nd = d + edge.weight\n      if nd < dist.get(edge.v, infinity):\n        dist[edge.v] = nd\n        pq.push((nd, edge.v))", ["Dijkstra com heap", "Dijkstra", "Bellman-Ford", "Prim MST"], "Dijkstra com heap", "hard", "grafos", "O(n log n)", "Fila de prioridade extrai vértice com menor distância provisória.", "pq.popMin + relaxação.", "https://www.geeksforgeeks.org/dsa/dijkstras-shortest-path-algorithm-greedy-algo-7/", "Dijkstra com min-heap: O((V+E) log V) em vez de O(V²) com matriz."),
  c("exo-as-rope-concat", "Rope concat", "function concatRope(left, right):\n  if left.weight < right.weight:\n    return new Node(left, right, left.length + right.length)\n  return new Node(right, left, left.length + right.length)\n\nfunction indexRope(node, i):\n  if i < node.left.length:\n    return indexRope(node.left, i)\n  return indexRope(node.right, i - node.left.length)", ["Rope (corda)", "Trie", "Segment tree", "Merge de ordenados"], "Rope (corda)", "hard", "arvore", "O(log n)", "Árvore balanceada de strings para concatenação e indexação logarítmica.", "Desce left ou right por peso.", "https://www.geeksforgeeks.org/dsa/rope-data-structure/", "Estrutura de corda (rope) para manipular strings grandes com concatenação eficiente."),
  c("exo-as-greedy-coins", "Greedy de moedas", "function minCoinsGreedy(coins, amount):\n  sort(coins descending)\n  count = 0\n  for coin in coins:\n    while amount >= coin:\n      amount = amount - coin\n      count = count + 1\n  return count", ["Greedy de moedas", "Coin change", "Seleção de atividades", "Counting sort"], "Greedy de moedas", "medium", "greedy", "O(n)", "Pega sempre a maior moeda possível — funciona para sistemas canônicos.", "while amount >= coin.", "https://www.geeksforgeeks.org/dsa/greedy-algorithm-to-find-minimum-number-of-coins/", "Algoritmo guloso que subtrai a maior moeda possível até zerar o valor."),
];
